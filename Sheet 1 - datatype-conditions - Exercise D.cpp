#include <iostream>
using namespace std;

int main() {
    long long a, b, c, d, x;
    cin >> a;
    cin >> b;
    cin >> c;
    cin >> d;
    x = (a*b) - (c*d);
    cout << "Difference = " << x;
    return 0;
}
